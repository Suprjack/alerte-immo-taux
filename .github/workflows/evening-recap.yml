name: 🌙 Récap Soir (19h00)

on:
  schedule:
    # Tous les jours à 19h (heure Paris = 18h UTC en hiver, 17h UTC en été)
    - cron: '0 18 * * *'
  workflow_dispatch: # Permet de lancer manuellement

permissions:
  contents: write

jobs:
  evening-recap:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          pip install -r scripts/requirements.txt

      - name: 🔍 Scraper - News, Trends & Sources Officielles
        run: |
          python scripts/scraper.py --mode evening

      - name: 📊 Génération Graphiques
        run: |
          python scripts/chart_generator.py

      - name: 🤖 Gemini - Génération Récap Soir (CTA Dynamique)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python scripts/rewriter.py --mode evening

      - name: 📤 Commit & Push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "L'Insider Bot"
          git add .
          git diff --staged --quiet || git commit -m "🌙 Récap Soir - $(date +'%Y-%m-%d')"
          git push

